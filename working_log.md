## 2026-09-13 19:30 KST — 감시 꺼짐 표시와 문서 현행화

- 감시가 꺼져 있을 때 프로그램 창 상단에 경고 배너를 띄우고 창 제목을 "TabBouncer - 감시 꺼짐"으로 바꿨다.
- 전용 Chrome 안내 페이지가 감시 상태를 CDP로 실시간 전달받아 표시하고, 꺼져 있으면 탭 제목 앞에 `[감시 꺼짐]`을 붙이게 했다.
- 설정 파일을 다시 읽으면 `enabled` 값으로 감시 상태가 덮어써지던 문제를 고쳐, 감시 켜짐·꺼짐은 GUI에서만 바뀌게 했다.
- 브라우저 스모크 테스트가 첫 탭이 `about:blank`인 동안 클릭을 시도해 실패하던 경쟁 조건을 고쳐 5/5 통과를 확인했다.
- README, 구현계획, 작업 로그를 현재 구현에 맞게 갱신했다.

## 2026-09-13 18:55 KST — 전용 Chrome 자동 재실행 중단과 실사용 오탐 수정

- 사용자가 평소 Chrome에서 사이트를 보고 있어 TabBouncer가 탭을 볼 수 없었고, 빈 전용 창을 닫을 때마다 5초 재연결 루프가 Chrome을 다시 띄우던 것이 "새 창 반복 생성"의 원인임을 확인했다.
- 전용 Chrome을 닫으면 다시 띄우지 않고 "Chrome 열기" 버튼을 기다리게 했으며, 실행 시 이 창에서만 차단된다는 안내 페이지를 열게 했다. `--restore-last-session`은 제거했다.
- 격리된 headless 하네스로 재현한 오탐을 고쳤다. 주소창·북마크 이동을 하이재킹으로 되돌리던 문제(`Page.frameRequestedNavigation`으로 페이지 주도 이동만 판정), 탭 상태를 15초 후 지워 오래 연 탭을 닫던 문제, 재연결 시 기존 탭을 새 팝업으로 닫던 문제, 반복 리다이렉트를 무한히 되돌리던 문제다.
- 수정 전 7/9 실패, 수정 후 9/9 통과를 확인했고 "Chrome 열기" 버튼은 UI 자동화로 검증했다.

## 2026-09-13 18:06 KST — 버튼으로 열리는 광고 도메인 추가

- 버튼 클릭 감점(-40) 때문에 `incompetencesorting.com` 광고 탭이 기준 점수에 못 미쳐 유지되던 것을 확인했다.
- 해당 도메인을 기본 `adDomains`에 추가하고 자체 테스트로 고정했다.

## 2026-09-13 17:37 KST — 앱 안 설정 편집 다이얼로그

- "설정 열기"가 외부 프로그램 대신 앱 안의 다이얼로그에서 `config.json`을 편집하게 했다.
- 저장 시 JSON을 검증한 뒤 바로 다시 적용하므로 "설정 다시 읽기" 버튼을 없앴다.

## 2026-09-13 17:14 KST — 현재 탭 리다이렉트 하이재킹 차단

- 새 탭 없이 보고 있던 탭 자체가 광고 사이트로 이동하는 경우를 실제 사이트에서 재현했다.
- 탭별 신뢰 URL을 두고, 클릭 없이 다른 사이트로 넘어가면 원래 URL로 되돌리는 판정을 추가했다(`blockRedirectHijack`).

## 2026-09-13 16:30 KST — 수동 감시 시작과 파일 로그

- 프로그램을 켜도 감시는 꺼진 상태로 시작하고 "감시 시작"을 눌러야 동작하게 했다. 자동화용 `--auto-start` 인수를 추가했다.
- 콘솔 출력을 모두 없애고 활동 로그를 `tabbouncer.log` 파일로 남기게 했다.

## 2026-09-13 16:15 KST — GUI 실행 직후 종료 수정

- 콘솔이 없는 GUI 실행에서 `Console.Title` 설정이 예외를 던져 창이 뜨기 전에 종료되던 문제를 고쳤다.

## 2026-09-13 16:09 KST — 빌드 출력 경로 정리

- 빌드 결과물이 프레임워크 버전 폴더 없이 저장소 루트의 `bin\Release`에 생성되게 했다.

## 2026-09-13 13:40 KST — 소스 구조 정리와 .NET 10 전환

- 루트의 소스와 프로젝트 파일을 `src`로 옮기고 대상 프레임워크를 `net10.0-windows`로 올렸다.
- README의 빌드 경로와 GUI 전환 후 남아 있던 콘솔 시절 안내를 바로잡았다.

## 2026-09-12 09:56 KST — GUI 변경 main 반영

- Windows GUI 전환 변경 파일의 범위와 diff 무결성을 확인했다.
- 구현 및 문서, 작업 로그를 하나의 커밋으로 정리했다.
- 원격 저장소의 main 브랜치에 변경 사항을 푸시했다.

## 2026-09-12 09:45 KST — Windows GUI 프로그램 전환

- 기존 콘솔 진입점을 Windows Forms 기반 단일 창으로 전환했다.
- 연결·감시·관측 상태와 최근 차단 내역, 활동 로그를 한 화면에 구성했다.
- 감시 제어, 탭 복구, 정상 사이트 등록, 설정 열기·재적용 기능을 GUI에 연결했다.
- README를 GUI 사용 방식에 맞게 수정하고 빌드 및 자체 테스트를 완료했다.
