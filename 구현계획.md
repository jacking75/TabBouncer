# TabBouncer — Chrome 광고 팝업 탭/창 자동 종료 프로그램

**통합 설계 · 구현 문서 (v3.0, 2026-09-13 현행화)**

| 항목 | 내용 |
|---|---|
| 프로젝트명 | **TabBouncer** (초대받지 않은 탭을 문 밖으로) |
| 대상 환경 | Windows 11 + Google Chrome (Stable 136 이상 기준) |
| 최종 채택 구현 | C# / .NET 10 Windows Forms GUI, CDP(Chrome DevTools Protocol) 방식 |
| 참고 구현 | Chrome 확장(MV3) + 네이티브 메시징 호스트(Go 또는 C#) |
| 외부 의존성 | 없음 (BCL의 `ClientWebSocket` + `System.Text.Json` + Windows Forms만 사용) |

> 이 문서는 콘솔 상주형 단일 파일로 설계한 v2.0에서 출발했다. §0~§3과 §9~§10, §14는 설계 당시의 검토 기록으로 그대로 두었고, 현재 동작은 §4~§8, §11~§13, §15와 README, `src/` 소스를 기준으로 한다. 이후 GUI 전환으로 소스가 세 파일로 나뉘었고, 사용자 클릭 의도 추적·현재 탭 리다이렉트 차단·수동 감시 시작이 추가됐으며, Dry-run 기본값·종료 횟수 제한·Chrome 자동 재실행·콘솔 핫키는 없어졌다.

---

## 0. 원래 요구사항

특정 사이트에서 어떤 동작을 하면 광고가 새로운 탭이나 웹브라우저 창으로 생성되어 몹시 불편하다. 이렇게 생성되는 탭이나 창을 자동으로 종료시켜 주는 프로그램을 만들고 싶다. Windows 11에서 Chrome을 사용하며, 가능하면 Go나 C#으로 구현한다. → 이후 **C#**, **단일 파일** 소스로 확정.

---

## 1. 문제 정의와 메커니즘 분석

자동 종료 로직을 설계하려면 "무엇을 탐지할 것인가"를 먼저 정확히 알아야 한다. 이런 광고 팝업이 만들어지는 대표적 패턴은 다음과 같다.

| 패턴 | 설명 | 탐지 단서 |
|---|---|---|
| **Pop-under / window.open** | 클릭 핸들러에서 `window.open()` 호출. 사용자 제스처가 있으므로 Chrome 기본 팝업 차단을 통과한다 | opener(부모 탭)가 존재, 생성 시각이 클릭 직후 |
| **`<a target="_blank">` 오버레이** | 투명 레이어에 광고 링크를 깔아둠 | 새 탭, opener 존재, 도메인이 원본과 다름 |
| **about:blank 후 리다이렉트** | 먼저 `about:blank`로 열고 JS로 광고 URL로 이동 | 생성 시 URL이 `about:blank`, 직후 외부 도메인으로 내비게이션 |
| **`window.open(...,'width=..,height=..')`** | 별도 창(popup type) 생성 | 창 크기가 작고 일반 창이 아님 |
| **`noopener` 체인 / 중간 리다이렉터** | 광고망 트래커 → 최종 랜딩 | 리다이렉트 체인 길이 2 이상, 첫 홉이 알려진 광고 도메인 |

핵심 신호는 결국 세 가지다. **(a) opener의 존재와 그 부모 탭의 도메인**, **(b) 새 탭/창의 최종 URL 도메인**, **(c) 생성 타이밍**. 이 세 가지를 얻을 수 있는 경로를 찾는 것이 아키텍처 선택의 전부다.

---

## 2. 구현 방식 비교와 최종 선택

### 2.1 후보 아키텍처

| # | 방식 | 정확도 | 안정성 | 단일 파일 가능 | 평소 프로필 사용 |
|---|---|---|---|---|---|
| **A** | Chrome 확장(MV3) + 네이티브 호스트(Go/C#) | ★★★★★ | ★★★★★ | ✕ (JS+manifest 필수) | ○ |
| **B** | **CDP 원격 제어 (C# 단독)** | ★★★★☆ | ★★★☆☆ | **○** | ✕ (전용 프로필) |
| **C** | Win32 창 감시(SetWinEventHook + UI Automation) | ★★☆☆☆ | ★★★☆☆ | ○ | ○ |
| **D** | 기존 광고차단기(uBlock Origin Lite 등) | ★★★☆☆ | ★★★★★ | — | ○ |

### 2.2 각 방식의 결정적 제약

**A안의 제약**: 정확도와 안정성은 최고이며 평소 쓰는 프로필을 그대로 감시할 수 있다. 그러나 확장은 `manifest.json` + JS 파일이 반드시 필요하므로 **C# 단일 파일 요구와 양립하지 않는다.** 또한 커맨드라인 `--load-extension` 플래그는 Chrome 137부터 브랜드 빌드에서 제거되어 스크립트 자동 로드가 불가능하고, `chrome://extensions` UI에서 수동 로드해야 한다.

**B안의 제약**: Chrome 136부터 `--remote-debugging-port` / `--remote-debugging-pipe` 스위치는 **기본 사용자 프로필 디렉터리에서는 무시된다.** (App-Bound Encryption 우회를 노린 정보탈취 악성코드의 쿠키 탈취 대응) 반드시 `--user-data-dir=<비표준 경로>`를 함께 줘야 하므로, 평소 프로필과 분리된 **전용 프로필**을 쓰게 된다.

**C안의 제약**: Chrome의 모든 창은 `Chrome_WidgetWin_1` 클래스이고 탭에는 윈도우 핸들이 아예 없다. 즉 새 "탭"은 Win32로 탐지 불가하고 새 "창"만 탐지 가능하다. URL도 UI Automation으로 주소창을 긁어야 하는데 느리고 깨지기 쉽다.

### 2.3 결론

**B안(CDP 단독, C# 단일 파일)을 최종 구현으로 채택한다.** C# 한 파일이라는 요구를 충족하는 유일한 고정확도 경로이며, 부수 효과로 **A안이 할 수 없는 일**을 하나 얻는다. `Target.setAutoAttach`의 `waitForDebuggerOnStart` 옵션으로 **새 탭이 스크립트를 단 한 줄도 실행하기 전에 일시정지**시켜 놓고 판정한 뒤 닫을 수 있어, 광고가 아예 렌더링되지 않는다.

A안은 §9에 참고 구현으로 남겨 둔다. 전용 프로필이 감수하기 어렵다면 그쪽으로 전환하면 된다.

---

## 3. 프로젝트 이름

| 이름 | 뉘앙스 | 실행 파일 |
|---|---|---|
| **TabBouncer** ⭐ 채택 | 클럽 문지기(bouncer)가 초대받지 않은 손님을 내보내는 이미지. 하는 일과 정확히 일치하고 발음도 좋다 | `tabbouncer.exe` |
| PopSweeper | 팝업을 쓸어버린다. 무해하고 설명이 필요 없음 | `popsweeper.exe` |
| TabReaper | "탭 사신". 강한 인상, 공격적인 느낌 | `tabreaper.exe` |
| Doorman / 문지기 | 조용하고 점잖은 느낌 | `doorman.exe` |
| WhackATab | 두더지잡기(whack-a-mole) 패러디. 유머러스 | `whackatab.exe` |

---

## 4. 아키텍처

### 4.1 채택안 (B: CDP 단독)

```
┌──────────────────────────────────────────────────────────────┐
│ tabbouncer.exe  (.NET 10 Windows Forms GUI)                  │
│                                                              │
│  ① 전용 프로필 Chrome 실행 (최초 1회 또는 "Chrome 열기")      │
│     --remote-debugging-port=9222                             │
│     --user-data-dir=%LOCALAPPDATA%\TabBouncer\ChromeProfile   │
│     시작 탭: 안내 페이지(start.html)                          │
│  ② /json/version → webSocketDebuggerUrl (브라우저 레벨 타깃)  │
│  ③ WebSocket 연결 후                                          │
│      Target.setDiscoverTargets{discover:true}                │
│      Target.getTargets  (연결 전부터 있던 탭 채택)            │
│      Target.setAutoAttach{flatten, waitForDebuggerOnStart}   │
│  ④ 탭마다 클릭 의도 추적 스크립트 주입 (Runtime.addBinding)   │
│  ⑤ 이벤트 구독                                                │
│      targetCreated / targetInfoChanged / attachedToTarget    │
│      Page.frameRequestedNavigation / frameNavigated          │
│      Network.requestWillBeSent / targetDestroyed             │
│  ⑥ 새 탭·창 점수 판정 → Target.closeTarget                   │
│     + Target.activateTarget 으로 opener 탭 포커스 복귀        │
│  ⑦ 페이지 주도 강제 이동 판정 → Page.navigate 로 원래 URL    │
│                                                              │
│  부가: GUI(감시 시작·관측 모드·Chrome 열기·설정 편집·복구),  │
│        config.json hot-reload, tabbouncer.log / events.jsonl │
└──────────────────────────────────────────────────────────────┘
                          │ WebSocket (127.0.0.1)
┌─────────────────────────▼────────────────────────────────────┐
│ Chrome (TabBouncer 전용 프로필)                              │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 참고안 (A: 확장 + 네이티브 호스트)

```
Chrome(평소 프로필)
 └ PopupGuard 확장(MV3 service worker)
     · chrome.tabs.onCreated / onUpdated
     · chrome.windows.onCreated
     · 1차 로컬 판정 → 즉시 tabs.remove
     · 애매한 건 네이티브 호스트에 질의
        │ Native Messaging (stdio, 4바이트 LE 길이 + UTF-8 JSON)
        ▼
   tabbouncer-host.exe (Go 또는 C#)
     · 규칙 엔진 / 로그 / 통계 / 설정 hot-reload
        │ Named Pipe
        ▼
   tabbouncer-tray.exe (선택) — 트레이 UI, Win32 폴백
```

호스트를 분리하는 이유는, MV3 서비스 워커가 유휴 시 종료되고 파일 시스템 접근이 없기 때문이다. 영구 로그, 사용자 편집 가능한 규칙 파일, 트레이 UI는 네이티브 쪽이 맞다. 네이티브 포트가 연결돼 있는 동안 서비스 워커가 살아 있게 유지되는 부수 효과도 있다.

---

## 5. 판정 엔진 설계

무조건 닫으면 정상 새 탭까지 죽는다. **점수제(scoring)** 로 설계한다.

### 5.1 수집 가능한 특징(feature)

| 특징 | CDP 출처 | 확장 출처 |
|---|---|---|
| 새 타깃 식별자 | `targetInfo.targetId` | `tabId`, `windowId` |
| opener 존재/식별자 | `targetInfo.openerId` | `tab.openerTabId` |
| opener URL | opener 타깃의 캐시된 `url` | `tabs.get(openerTabId).url` |
| 초기 URL | `targetCreated` 시점의 `url` | `tab.pendingUrl` |
| 최종 URL | `targetInfoChanged` / `Page.frameNavigated` | `tabs.onUpdated` |
| 창 유형/크기 | `Browser.getWindowForTarget().bounds` | `windows.get().type/width/height` |
| 백그라운드 생성 | (간접) | `tab.active === false` |
| 클릭→생성 Δt | 프로그램 기준 생성 시각 | content script의 클릭 타임스탬프 |
| 리다이렉트 체인 | `Page.frameNavigated` 횟수 | `webNavigation.onCommitted`의 `transitionQualifiers` |
| 사용자 클릭 의도 | `Runtime.addBinding` + 주입 스크립트가 보내는 클릭 대상·목적지 | content script |
| 페이지 주도 이동 여부 | `Page.frameRequestedNavigation` 수신 여부 | `webNavigation.onCommitted`의 `transitionType` |

### 5.2 룰셋

새 탭·창 판정(`Score`)은 다음과 같다. 클릭 의도(intent)의 분류는 §5.4를 따른다.

```
# 0단계: 범위 제한 (가장 중요한 안전장치)
if 명시적으로 클릭한 링크·폼 목적지와 일치      → -999 (explicit-user-navigation)
if URL이 blank                                  → -999 (blank-pending, 이동 후 재판정)
if URL이 내부 페이지(chrome://, file:// 등)     → -999 (internal)
if 목적지가 allowedSites / whitelist 매칭        → -999
if URL이 로그인·OAuth·결제 패턴                  → -999 (sensitive-flow)
if opener 없음 AND 목적지가 광고망 아님          → -999 (no-opener: 주소창·새 탭 버튼)
if opener가 watchedSites 밖 AND 목적지가 광고망 아님
   AND (사용자 동작 없음·엉뚱한 클릭으로 열린 크로스 사이트 탭)도 아님
   AND !strictMode                               → -999 (not-in-scope)

# 1단계: 가점/감점
+80  목적지가 adDomains 매칭 (ad-domain)
+45  opener 호스트가 watchedSites 매칭 (watched-opener)
+60  다른 목적지를 클릭했거나 빈 영역·투명 링크를 클릭 (unexpected)
+50  사용자 동작 없음 (no-user-gesture)
+25  목적지 eTLD+1 ≠ opener eTLD+1 (cross-site)
-35  목적지 eTLD+1 == opener eTLD+1 (same-site)
+15  창 크기가 팝업급, 가로<1000 또는 세로<780 (popup-window)
+20  opener 자체가 광고 도메인 (ad-chain)
+15  초기 URL이 about:blank 였고 이후 외부로 이동 (blank-redirect)
+15  목적지 TLD가 스팸 다발 TLD (.top .xyz .buzz .click …)
+10  생성 후 1.5초 안에 판정 (fast-open)
+10  리다이렉트 체인 ≥ 2 (redirect-chain)
-40  버튼 등 명시적 컨트롤 클릭 (clicked-control)

# 2단계: 액션
score >= closeThreshold(기본 80)  → close (관측 모드면 기록만)
score >= closeThreshold - 30      → 회색지대: events.jsonl에 기록
그 외                              → keep
```

### 5.3 안전장치 (필수)

정상 탭을 잡아먹는 것이 이런 도구의 가장 큰 실패 모드라서 여러 겹으로 막는다. 가장 중요한 것은 위 0단계의 **범위 제한**이고, 그 위에 다음 장치를 둔다.

- **감시는 수동으로 시작한다.** 프로그램을 켜면 항상 꺼진 상태이고, GUI에서 "감시 시작"을 눌러야 닫기·되돌리기가 동작한다. 꺼져 있는 동안에는 GUI 상단 배너, 창 제목, 전용 Chrome 안내 페이지에 표시한다. 설정 파일을 다시 읽어도 이 상태는 바뀌지 않는다.
- **관측 모드(dryRun)**에서는 판정만 기록하고 닫거나 되돌리지 않는다.
- **명시적 클릭 보호.** 눈에 보이는 링크·폼을 실제로 클릭해 연 탭은 목적지를 대조해 유지한다. 목적지가 광고 도메인이어도 닫지 않는다.
- **창에 남은 마지막 일반 탭은 닫지 않는다**(브라우저 통째 종료 사고 방지).
- **연결 전부터 열려 있던 탭은 새 팝업으로 보지 않는다.** TabBouncer를 재시작해 기존 Chrome에 붙어도 열려 있던 탭을 닫지 않는다.
- 닫거나 되돌린 최근 20건 중 가장 최근 항목은 GUI에서 다시 열 수 있고, 그 도메인을 즉시 `allowedSites`에 등록할 수 있다.
- `chrome://*`, `chrome-extension://*`, `devtools://`, `file://`은 대상에서 원천 제외하며, 로그인·결제·OAuth 경로는 화이트리스트와 URL 패턴 검사로 이중 보호한다.

종료 횟수 제한은 없다. 초기 설계에 있던 "Dry-run 기본값"과 "10초에 5건 초과 시 Dry-run 후퇴"는 현재 구현에 없으며, 짧은 시간에 광고 탭이 10개 이상 생겨도 광고로 판정되는 항목은 모두 닫는다.

### 5.4 사용자 클릭 의도 수집

각 탭에 `Runtime.addBinding`으로 `__tabBouncerIntent` 함수를 만들고, 신뢰된(`isTrusted`) 포인터 클릭·보조 클릭·키보드 링크 실행·폼 제출을 캡처 단계에서 기록하는 짧은 스크립트를 `Page.addScriptToEvaluateOnNewDocument`로 주입한다. 스크립트는 클릭 대상이 눈에 보이고 설명(텍스트·aria-label·alt)이 있는 링크인지, 보이지 않는 링크인지, 버튼 같은 컨트롤인지, 빈 영역인지를 구분해 목적지 URL과 함께 보낸다.

판정 때는 `intentWindowMs`(기본 3.5초) 안의 **가장 최근 제스처 하나만** 본다. 새 탭·창이면 opener 탭의 기록을, 현재 탭 이동이면 그 탭 자신의 기록을 쓴다.

| 최근 제스처 | 분류 |
|---|---|
| 보이는 링크·폼 클릭, 목적지 일치(추적 쿼리 추가 허용) | explicit — 보호 |
| 보이는 링크·폼 클릭, 목적지 불일치 | clicked-url-mismatch — unexpected |
| 빈 영역·보이지 않는 링크 클릭 | passive-click-popup — unexpected |
| 버튼 등 컨트롤 클릭 | clicked-control — 감점 |
| 기록 없음 | no-user-gesture — automatic |

### 5.5 현재 탭 리다이렉트 하이재킹

새 탭을 만들지 않고 **보고 있던 탭 자체**를 광고 사이트로 보내는 방식에 대응한다(`ScoreRedirectHijack`).

- 탭마다 마지막으로 머문 URL을 신뢰 기준으로 기록한다. 연결 시점에 열려 있던 탭은 현재 URL로 시작한다.
- **페이지가 시작한 이동만 판정한다.** Chrome은 스크립트·링크·meta refresh가 요청한 이동에만 `Page.frameRequestedNavigation`을 보내고, 주소창 입력·북마크·뒤로 가기에는 보내지 않는다. 이 이벤트가 최근 10초 안에 온 이동만 대상으로 삼고, 나머지는 신뢰 기준만 옮긴다.

```
# 0단계
if 명시적으로 클릭한 링크·폼 목적지와 일치     → -999
if 목적지가 allowedSites / whitelist / 로그인·결제 패턴 → -999
if 같은 eTLD+1 안에서의 이동                    → -999

# 1단계
+80  사용자 동작 없음
+80  다른 목적지를 클릭했거나 빈 영역·보이지 않는 링크를 클릭
+20  목적지가 adDomains 매칭
+15  출발 사이트가 watchedSites 매칭
+15  목적지 TLD가 스팸 다발 TLD
-60  버튼 등 명시적 컨트롤 클릭

# 2단계
score >= closeThreshold → Page.navigate 로 신뢰 URL 복귀 (관측 모드면 기록만)
```

되돌린 페이지가 스스로 다시 이동하면 무한 반복되므로, 같은 신뢰 URL에서 30초 안에 두 번 되돌렸다면 그다음 이동은 허용한다. 이 기능은 `blockRedirectHijack: false`로 끌 수 있다.

---

## 6. 최종 구현 — 소스 구조

초기 설계는 콘솔에 상주하는 단일 파일 소스였지만, Windows GUI로 전환하면서 현재 소스는 세 파일로 나뉜다. 설계 당시의 단일 파일 소스 전문은 저장소 이력(`git log -- 구현계획.md`)에서 볼 수 있고, 현재 동작의 기준은 `src/` 소스다.

| 파일 | 역할 |
|---|---|
| `src/Program.cs` | 진입점, 설정 로드, Chrome 실행·연결, CDP 클라이언트, 이벤트 처리, 판정 엔진, 자체 테스트 |
| `src/MainForm.cs` | 메인 창. 연결·감시 상태, 감시 시작/일시중지, 관측 모드, Chrome 열기, 최근 차단 목록, 활동 로그 |
| `src/ConfigForm.cs` | 앱 안에서 `config.json`을 편집·검증·저장하는 다이얼로그 |
| `src/config.json` | 기본 설정 템플릿. 빌드 시 실행 파일 옆으로 복사된다 |
| `tests/browser-smoke.mjs` | 실제 headless Chrome으로 5개 시나리오를 확인하는 스모크 테스트 |

### 6.1 실행 흐름

1. `Main`은 `--self-test`가 있으면 판정 점수 단위 검사만 돌리고 끝낸다.
2. 데이터 디렉터리(`--data-dir` 또는 `%LOCALAPPDATA%\TabBouncer`)를 만들고 실행 파일 옆 `config.json`을 읽는다. 감시는 항상 꺼진 상태로 시작한다(`--auto-start`만 예외).
3. GUI 창을 띄우고, 백그라운드에서 `SweepAsync`(정지된 탭 재개·지연 판정), `RunEngineAsync`(Chrome 연결 루프), 설정 파일 감시를 시작한다.
4. `RunSessionAsync`는 디버깅 포트에 Chrome이 있으면 붙고, 없으면 처음 한 번만(또는 사용자가 "Chrome 열기"를 누를 때) 전용 프로필로 Chrome을 띄운다. 연결 후 `Target.setDiscoverTargets` → `Target.getTargets`로 기존 탭 채택 → `Target.setAutoAttach{flatten, waitForDebuggerOnStart=preemptiveBlock}` 순으로 설정한다.
5. 연결이 끊기면 `WaitForChromeAsync`가 1초 뒤 디버깅 포트를 확인한다. Chrome이 살아 있으면 다시 붙고, 없으면 "Chrome 닫힘" 상태로 사용자의 요청을 기다린다.

### 6.2 주요 구성 요소

| 구성 요소 | 내용 |
|---|---|
| `CdpClient` | `ClientWebSocket` 기반 CDP 클라이언트. 명령 id 매칭, 타임아웃, 이벤트 발행 |
| `OnCdpEvent` | `Target.*`, `Runtime.bindingCalled`, `Page.frameRequestedNavigation`, `Page.frameNavigated`, `Page.loadEventFired`, `Network.requestWillBeSent`를 각 처리기로 분배 |
| `InitializeAttachedPageAsync` | 새 탭이 정지 상태면 선판정 후 재개하고, `Runtime.addBinding`과 클릭 의도 추적 스크립트를 주입 |
| `EvaluateAsync` + `Score` | 새 탭·창 판정(§5.2). 기준 이상이면 `Target.closeTarget` |
| `EvaluateRedirectHijackAsync` + `ScoreRedirectHijack` | 현재 탭 강제 이동 판정(§5.5). 기준 이상이면 `Page.navigate`로 복귀 |
| `LaunchChrome` / `WriteGuidePage` | 전용 프로필 Chrome 실행, 안내 페이지(`start.html`) 생성 |
| `PushMonitoringStateToGuide` | 안내 페이지에 감시 켜짐·꺼짐 상태를 실시간 전달 |

---

## 7. 빌드와 첫 실행

### 7.1 빌드

```powershell
dotnet build src -c Release
dotnet publish src -c Release -r win-x64 --self-contained false
```

빌드 결과는 `bin\Release\tabbouncer.exe`, 게시 결과는 `bin\Release\win-x64\publish\tabbouncer.exe`다. `src/TabBouncer.csproj`는 `net10.0-windows`, `UseWindowsForms`, `OutputType=WinExe`이고, 출력 경로를 저장소 루트 `bin\`에 프레임워크 버전 폴더 없이 두도록 설정돼 있다. NuGet 패키지는 쓰지 않는다.

### 7.2 첫 실행 순서

1. `tabbouncer.exe`를 실행하면 GUI 창이 뜨고, 전용 프로필 Chrome이 안내 페이지와 함께 열린다. 이때 감시는 꺼져 있어 GUI 상단에 경고 배너가 보이고, 안내 페이지 탭 제목에는 `[감시 꺼짐]`이 붙는다.
2. GUI에서 **감시 시작**을 누른다. 배너가 사라지고 안내 페이지가 "감시 중"으로 바뀐다.
3. 보고 싶은 사이트는 **전용 Chrome 창의 주소창**에서 연다. 평소 쓰는 Chrome 창은 보호되지 않는다.
4. 오탐이 걱정되면 먼저 **관측 모드**를 켜고 사이트에서 광고 팝업을 몇 번 유발해 본 뒤, `events.jsonl`에서 정상 탭이 대상으로 잡히지 않는지 확인하고 관측 모드를 끈다.
5. 전용 창을 모두 닫으면 Chrome은 다시 뜨지 않는다. 다시 감시하려면 **Chrome 열기**를 누른다.

### 7.3 설정 예시

```jsonc
// 실행 파일과 같은 폴더의 config.json (--data-dir을 주면 그 폴더)  (요점만)
{
  "dryRun": false,
  "strictMode": false,
  "preemptiveBlock": true,
  "protectExplicitClicks": true,
  "blockAutomaticCrossSitePopups": true,
  "blockRedirectHijack": true,
  "closeThreshold": 80,
  "debugPort": 9222,
  "watchedSites": [ "problem-site.com" ],
  "allowedSites": [ "my-safe-site.com" ],
  "whitelist": [ "accounts.google.com", "toss.im" ],
  "startUrl": ""
}
```

`enabled` 값은 무시된다. 감시는 GUI에서만 켜고 끈다. `startUrl`이 비어 있으면 안내 페이지를 연다. 설정은 GUI의 "설정 열기"에서 편집할 수 있고, 저장하면 JSON을 검증한 뒤 바로 반영한다.

`adDomains`는 직접 관리해도 되지만, EasyList / EasyPrivacy의 도메인 목록을 하루 1회 내려받아 파싱해 병합하면 정확도가 크게 오른다.

### 7.4 커맨드라인 인수와 GUI 조작

| 인수 | 의미 |
|---|---|
| `--live` / `--dry-run` | 실제 종료 / 관측 전용 |
| `--strict` | watchedSites 밖도 검사 |
| `--no-preempt` | 선차단 비활성(새 탭 정지 없음) |
| `--port=9222` | 디버깅 포트 |
| `--url=https://…` | Chrome 시작 페이지 |
| `--data-dir=경로` | 설정·로그·전용 프로필 위치 분리 |
| `--auto-start` | 감시를 켠 상태로 시작(자동화 테스트용) |
| `--self-test` | 판정 점수 단위 검사만 실행 |

초기 설계의 콘솔 핫키는 GUI 조작으로 바뀌었다.

| 조작 | 동작 |
|---|---|
| 감시 시작 / 감시 일시중지 | 닫기·되돌리기 켜기/끄기 |
| 관측 모드 체크 | 닫지 않고 판정만 기록 |
| Chrome 열기 | 전용 Chrome이 닫혀 있을 때 다시 실행 |
| 설정 열기 | `config.json` 편집·검증·저장(저장 즉시 반영) |
| 다시 열기 | 가장 최근에 차단한 탭 복구 |
| 정상 사이트로 등록 | 가장 최근 차단 도메인을 `allowedSites`에 추가 |

---

## 8. 구현상 특별히 신경 쓴 부분

**선차단(preemptive) 모드의 위험 관리.** `waitForDebuggerOnStart`는 **모든** 새 탭을 정지시키므로, 판정 로직이 늦어지면 사용자가 직접 연 탭도 멈춘 것처럼 보인다. 그래서 정지된 탭은 클릭 의도 이벤트가 먼저 도착하도록 150ms만 기다린 뒤 네트워크 호출 없는 순수 계산으로 판정하고, `finally`에서 무조건 `Runtime.runIfWaitingForDebugger`를 호출한다. 그것마저 실패한 경우를 대비해 스위퍼 루프가 1.6초 넘게 정지된 세션을 강제로 풀어준다. 창 크기 조회처럼 왕복이 필요한 작업은 정지 상태가 아닌 debounce 단계로 미룬다.

**about:blank 팝언더 대응.** 생성 시 URL이 `about:blank`면 판정을 미루고 `Page.enable`로 해당 세션의 내비게이션을 구독한다. 이후 `Page.frameNavigated`가 최상위 프레임에서 외부 도메인을 알려주는 순간 즉시 재판정한다. 동시에 `debounceMs`(기본 1200ms) 타이머가 백스톱으로 동작한다.

**중복 처리 방지.** 하나의 타깃에 대해 `targetCreated`, `attachedToTarget`, `targetInfoChanged`, `frameNavigated`, `debounce` 등 여러 경로로 판정이 들어올 수 있으므로 `Interlocked.Exchange(ref ctx.Decided, 1)`로 실제 액션은 정확히 한 번만 실행된다. 한 번 유지로 결정된 탭은 새 팝업 판정을 다시 받지 않지만, 현재 탭 강제 이동 판정(§5.5)은 이 결정과 별도로 이동마다 받는다.

**탭 상태의 수명.** 탭별 판정 상태(`PageContext`)는 `Target.targetDestroyed`가 올 때까지 유지한다. 초기에는 생성 15초 뒤 지웠는데, 그러면 오래 열어 둔 탭이 다음 이동 때 새 팝업처럼 다시 판정돼 닫히고 리다이렉트 차단 기준도 사라져 없앴다.

**Chrome 종료와 재연결.** Chrome이 닫혀 WebSocket이 끊기면 1초 뒤 디버깅 포트를 확인해 Chrome이 살아 있으면 다시 붙고, 없으면 사용자가 "Chrome 열기"를 누를 때까지 기다린다. 초기 설계의 "5초 후 재탐색·재실행"은 사용자가 전용 창을 닫을 때마다 새 창이 되살아나는 문제를 만들어 없앴다. 재연결할 때는 `Target.getTargets`로 이미 열려 있던 탭을 판정 완료 상태로 채택해, 기존 탭을 새 팝업으로 오인하지 않는다.

**안내 페이지와 감시 상태.** 안내 페이지는 로컬 파일이라 감시 상태를 스스로 알 수 없다. 페이지 로드(`Page.loadEventFired`), 탭 연결, 감시 전환 때마다 `Runtime.evaluate`로 상태를 전달해 화면과 탭 제목을 갱신한다.

---

## 9. 참고 구현 A안 — Chrome 확장(MV3) + 네이티브 호스트

전용 프로필이 받아들이기 어려워 평소 프로필을 그대로 감시해야 한다면 이 구조로 전환한다. 단일 파일 조건은 포기해야 한다.

### 9.1 확장 구조

```
tabbouncer-extension/
├── manifest.json
├── background.js          # service worker
├── click-tracker.js       # content script (클릭 타이밍 기록)
├── options.html / options.js
├── popup.html / popup.js  # 최근 차단 목록 & 복구
└── key.pem                # 확장 ID 고정용 (저장소에 커밋 금지)
```

`manifest.json`

```json
{
  "manifest_version": 3,
  "name": "TabBouncer",
  "version": "0.1.0",
  "minimum_chrome_version": "116",
  "key": "MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8A...(9.4절 참조)",
  "permissions": ["tabs","webNavigation","storage","alarms","sessions","notifications","nativeMessaging"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js", "type": "module" },
  "content_scripts": [{
    "matches": ["<all_urls>"], "js": ["click-tracker.js"],
    "run_at": "document_start", "all_frames": true
  }],
  "action": { "default_popup": "popup.html" },
  "options_page": "options.html"
}
```

`background.js` (핵심)

```js
const DEFAULTS = {
  enabled: true, dryRun: true, strictMode: false, closeThreshold: 70,
  watchedSites: [], adDomains: [], whitelist: ["accounts.google.com"],
  useNativeHost: true, rateLimit: { windowMs: 10000, max: 5 }
};
let cfg = { ...DEFAULTS };
const pending = new Map();
const lastClick = new Map();     // tabId -> {ts, userIntent}
const exempt   = new Map();      // Ctrl+클릭 등 사용자 의도 면제
let recentClosed = [], closeTimestamps = [];

const host = u => { try { return new URL(u).hostname; } catch { return ""; } };
function base(h) {
  const p = h.split("."), two = new Set(["co","com","ne","or","go","ac","gov","edu"]);
  return (p.length >= 3 && two.has(p[p.length-2])) ? p.slice(-3).join(".") : p.slice(-2).join(".");
}
const matchList = (h, list) => !!h && list.some(d => h === d || h.endsWith("." + d));

// ---- 네이티브 호스트 ----
let port = null; const waiters = new Map(); let seq = 0;
function connectHost() {
  if (!cfg.useNativeHost) return;
  try {
    port = chrome.runtime.connectNative("com.tabbouncer.host");
    port.onMessage.addListener(msg => {
      if (msg.type === "config") { cfg = { ...cfg, ...msg.config }; return; }
      const w = waiters.get(msg.id);
      if (w) { waiters.delete(msg.id); clearTimeout(w.t); w.resolve(msg); }
    });
    port.onDisconnect.addListener(() => { port = null; setTimeout(connectHost, 5000); });
    port.postMessage({ type: "hello", version: chrome.runtime.getManifest().version });
  } catch { port = null; }
}
function askHost(payload, timeoutMs = 300) {
  if (!port) return Promise.resolve(null);
  const id = ++seq;
  return new Promise(resolve => {
    const t = setTimeout(() => { waiters.delete(id); resolve(null); }, timeoutMs);
    waiters.set(id, { resolve, t });
    port.postMessage({ ...payload, id });
  });
}

// ---- 클릭 신호 ----
chrome.runtime.onMessage.addListener((m, sender) => {
  if (m?.type !== "click" || !sender.tab) return;
  lastClick.set(sender.tab.id, { ts: Date.now(), userIntent: !!m.userIntent });
  if (m.userIntent) exempt.set(sender.tab.id, Date.now() + 5000);   // 5초간 면제
});

// ---- 탭 생성 ----
chrome.tabs.onCreated.addListener(async tab => {
  if (!cfg.enabled) return;
  const ctx = {
    tabId: tab.id, windowId: tab.windowId, createdAt: Date.now(),
    openerTabId: tab.openerTabId ?? null,
    initialUrl: tab.pendingUrl || tab.url || "",
    active: tab.active, openerUrl: "", windowType: "", finalUrl: "",
    redirects: 0, dtClick: Infinity, exempt: false
  };
  if (ctx.openerTabId != null) {
    const ex = exempt.get(ctx.openerTabId);
    if (ex && ex > Date.now()) ctx.exempt = true;          // Ctrl+클릭 등
    try { const op = await chrome.tabs.get(ctx.openerTabId);
          ctx.openerUrl = op.url || op.pendingUrl || ""; } catch {}
    const c = lastClick.get(ctx.openerTabId);
    if (c) ctx.dtClick = ctx.createdAt - c.ts;
  }
  try { const w = await chrome.windows.get(tab.windowId);
        ctx.windowType = w.type; ctx.winSize = { w: w.width, h: w.height }; } catch {}

  pending.set(tab.id, ctx);
  await evaluate(ctx, "onCreated");
  setTimeout(() => { if (pending.has(tab.id)) evaluate(pending.get(tab.id), "timeout"); }, 1500);
  setTimeout(() => pending.delete(tab.id), 8000);
});

chrome.tabs.onUpdated.addListener((tabId, info, tab) => {
  const ctx = pending.get(tabId); if (!ctx) return;
  if (info.url || info.status === "loading") {
    ctx.finalUrl = tab.pendingUrl || tab.url || ctx.finalUrl;
    if (ctx.finalUrl && !ctx.finalUrl.startsWith("about:")) evaluate(ctx, "onUpdated");
  }
});

chrome.webNavigation.onCommitted.addListener(d => {
  if (d.frameId !== 0) return;
  const ctx = pending.get(d.tabId); if (!ctx) return;
  if (d.transitionQualifiers?.some(q => q.includes("redirect"))) ctx.redirects++;
});

chrome.tabs.onRemoved.addListener(id => {
  pending.delete(id); lastClick.delete(id); exempt.delete(id);
});

// ---- 점수 ----
function score(ctx) {
  const url = ctx.finalUrl || ctx.initialUrl;
  const h = host(url), oh = host(ctx.openerUrl);
  if (!url || url.startsWith("chrome://") || url.startsWith("chrome-extension://")) return -999;
  if (ctx.exempt) return -999;
  if (matchList(h, cfg.whitelist)) return -999;

  let s = 0;
  if (oh && matchList(oh, cfg.watchedSites)) s += 50;
  else if (!cfg.strictMode) return -999;

  if (matchList(h, cfg.adDomains)) s += 40;
  if (h && oh && base(h) !== base(oh)) s += 25; else s -= 30;
  if (ctx.windowType === "popup") s += 20;
  if (ctx.active === false) s += 15;
  if (!ctx.initialUrl || ctx.initialUrl.startsWith("about:")) s += 15;
  if (ctx.dtClick < 1500) s += 10;
  if (ctx.redirects >= 2) s += 10;
  return s;
}

// ---- 판정/실행 ----
async function evaluate(ctx, stage) {
  if (ctx.done) return;
  let s = score(ctx), reason = `local(${stage})`;
  if (s > -900 && s < cfg.closeThreshold && s >= cfg.closeThreshold - 40) {
    const r = await askHost({ type: "evaluate", ctx });
    if (r && typeof r.score === "number") { s = r.score; reason = "host:" + (r.reason || ""); }
  }
  port?.postMessage({ type: "log", entry: { ...ctx, stage, score: s, reason } });
  if (s < cfg.closeThreshold) return;
  ctx.done = true;
  if (cfg.dryRun) return notify("(관측 모드) 차단 대상", ctx.finalUrl || ctx.initialUrl);
  if (!allowRate()) { cfg.dryRun = true; return notify("차단 급증", "관측 모드로 전환합니다."); }
  await closeTab(ctx, s, reason);
}

function allowRate() {
  const now = Date.now();
  closeTimestamps = closeTimestamps.filter(t => now - t < cfg.rateLimit.windowMs);
  if (closeTimestamps.length >= cfg.rateLimit.max) return false;
  closeTimestamps.push(now); return true;
}

async function closeTab(ctx, s, reason) {
  const url = ctx.finalUrl || ctx.initialUrl;
  try {
    try { await chrome.tabs.update(ctx.tabId, { url: "about:blank" }); } catch {}  // beforeunload 회피
    await chrome.tabs.remove(ctx.tabId);
  } catch {
    try {
      const w = await chrome.windows.get(ctx.windowId, { populate: true });
      if (w.tabs.length <= 1 && w.type === "popup") await chrome.windows.remove(ctx.windowId);
    } catch {}
  }
  recentClosed.unshift({ url, opener: ctx.openerUrl, score: s, reason, at: Date.now() });
  recentClosed = recentClosed.slice(0, 20);
  await chrome.storage.local.set({ recentClosed });
  port?.postMessage({ type: "closed", url, opener: ctx.openerUrl });
  if (ctx.openerTabId != null) {
    try {
      const op = await chrome.tabs.get(ctx.openerTabId);
      await chrome.windows.update(op.windowId, { focused: true });
      await chrome.tabs.update(op.id, { active: true });
    } catch {}
  }
}

function notify(title, message) {
  chrome.notifications.create({ type: "basic", iconUrl: "icon128.png", title, message: message || "" });
}

chrome.runtime.onStartup.addListener(init);
chrome.runtime.onInstalled.addListener(init);
async function init() {
  const st = await chrome.storage.sync.get("cfg");
  cfg = { ...DEFAULTS, ...(st.cfg || {}) };
  connectHost();
}
init();
```

`click-tracker.js`

```js
function send(e) {
  const userIntent = !!(e.ctrlKey || e.metaKey || e.shiftKey || e.button === 1);
  try { chrome.runtime.sendMessage({ type: "click", userIntent }); } catch {}
}
window.addEventListener("pointerdown", send, true);
window.addEventListener("click", send, true);
window.addEventListener("auxclick", send, true);
```

> `Ctrl+클릭`이나 마우스 가운데 버튼은 사용자가 **의도적으로** 새 탭을 여는 행위다. 이를 구분해 해당 opener에서 5초간 생성되는 탭을 판정 면제 처리하면 오탐이 거의 사라진다. 설계 당시에는 CDP 방식에서 이 신호를 얻을 수 없다고 봤고, 이것이 A안의 가장 큰 정확도 우위였다. 현재 B안 구현은 각 탭에 클릭 의도 추적 스크립트를 주입해 같은 신호를 얻는다(§5.4).

### 9.2 네이티브 메시징 프로토콜

Chrome ↔ 호스트는 stdin/stdout으로 통신하며, 각 메시지는 **4바이트 길이(리틀엔디언) + UTF-8 JSON 본문**이다. 확장→호스트 최대 4GB, 호스트→확장 최대 1MB. **stdout에 로그를 쓰면 프로토콜이 깨지므로** 로그는 반드시 파일이나 stderr로 보내야 한다.

호스트 매니페스트 `C:\Program Files\TabBouncer\com.tabbouncer.host.json`

```json
{
  "name": "com.tabbouncer.host",
  "description": "TabBouncer native host",
  "path": "C:\\Program Files\\TabBouncer\\tabbouncer-host.exe",
  "type": "stdio",
  "allowed_origins": [ "chrome-extension://abcdefghijklmnopabcdefghijklmnop/" ]
}
```

레지스트리 등록

```powershell
New-Item -Path "HKCU:\Software\Google\Chrome\NativeMessagingHosts\com.tabbouncer.host" -Force |
  Set-ItemProperty -Name "(default)" -Value "C:\Program Files\TabBouncer\com.tabbouncer.host.json"
```

### 9.3 호스트 프레이밍 코드

**C#** — 판정 로직(`Score`, `HostOf`, `Etld1`, `Match`)은 §6의 것을 그대로 재사용하고, 입출력만 아래로 바꾼다.

```csharp
static Stream _in = Console.OpenStandardInput();
static Stream _out = Console.OpenStandardOutput();

static byte[]? ReadMessage()
{
    var len = new byte[4];
    if (!ReadExact(len, 4)) return null;
    int n = BinaryPrimitives.ReadInt32LittleEndian(len);
    if (n <= 0 || n > 64 * 1024 * 1024) return null;
    var buf = new byte[n];
    return ReadExact(buf, n) ? buf : null;
}
static bool ReadExact(byte[] buf, int count)
{
    int off = 0;
    while (off < count) { int r = _in.Read(buf, off, count - off); if (r <= 0) return false; off += r; }
    return true;
}
static void Write(object o)
{
    var json = JsonSerializer.SerializeToUtf8Bytes(o);
    lock (_outLock)
    {
        var len = new byte[4];
        BinaryPrimitives.WriteInt32LittleEndian(len, json.Length);
        _out.Write(len); _out.Write(json); _out.Flush();
    }
}
```

`<OutputType>WinExe</OutputType>`로 콘솔 창을 숨기되 `Console.WriteLine`은 **절대 사용 금지**다.

**Go** — 동일 규격.

```go
func readMsg(r io.Reader) ([]byte, error) {
	var n uint32
	if err := binary.Read(r, binary.LittleEndian, &n); err != nil { return nil, err }
	if n == 0 || n > 64*1024*1024 { return nil, io.ErrUnexpectedEOF }
	buf := make([]byte, n)
	_, err := io.ReadFull(r, buf)
	return buf, err
}

func writeMsg(v interface{}) {
	b, err := json.Marshal(v); if err != nil { return }
	outMu.Lock(); defer outMu.Unlock()
	_ = binary.Write(os.Stdout, binary.LittleEndian, uint32(len(b)))
	_, _ = os.Stdout.Write(b)
}
```

빌드: `go build -ldflags "-s -w -H=windowsgui" -o tabbouncer-host.exe .` (디버깅 중에는 `-H=windowsgui`를 빼는 편이 낫다.)

메시지 타입은 `hello`(→`config` 응답), `evaluate`(→`verdict{score,reason}`), `log`, `closed` 네 가지면 충분하다. 설정 파일은 `fsnotify` / `FileSystemWatcher`로 감시해 변경 시 확장에 `config`를 푸시한다.

### 9.4 확장 설치와 ID 고정

`chrome://extensions` → 개발자 모드 ON → "압축해제된 확장 프로그램을 로드". 커맨드라인 `--load-extension` 플래그는 Chrome 137부터 브랜드 빌드에서 제거되었으므로 UI 로드 또는 정책 방식만 유효하다.

압축해제 확장은 경로에 따라 ID가 바뀌어 네이티브 메시징의 `allowed_origins`가 깨진다. 고정하려면 "확장 프로그램 압축"으로 `.crx` + `.pem`을 만들고, 공개키를 매니페스트 `key` 필드에 넣는다.

```powershell
openssl rsa -in key.pem -pubout -outform DER | openssl base64 -A
```

재부팅 후에도 "개발자 모드 확장" 경고를 없애고 싶으면 `HKLM\SOFTWARE\Policies\Google\Chrome\ExtensionSettings` 정책으로 등록한다.

### 9.5 트레이 앱과 자동시작

네이티브 호스트는 Chrome이 켜질 때 Chrome이 직접 실행/종료하므로 자동시작 등록이 불필요하다. 상시 UI가 필요하면 별도 트레이 프로세스를 두고 Named Pipe(`\\.\pipe\tabbouncer`)로 통신한다. C#은 `NotifyIcon` + `ContextMenuStrip`, Go는 `getlantern/systray`를 쓴다. 트레이 앱만 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 또는 작업 스케줄러(로그온 트리거)에 등록한다.

---

## 10. 보조 방어선 (선택)

### 10.1 Chrome 정책으로 팝업 자체 차단

```
HKLM\SOFTWARE\Policies\Google\Chrome\PopupsBlockedForUrls
  1 = "[*.]problem-site.com"

HKLM\SOFTWARE\Policies\Google\Chrome\URLBlocklist
  1 = "popads.net"
  2 = "*.adsterra.com"
```

적용 후 `chrome://policy`에서 "정책 다시 로드"로 확인한다. 다만 사이트가 팝업 실패를 감지해 콘텐츠를 막는 경우가 있어, 그럴 때는 "열리게 두고 즉시 닫기"(본 프로그램 방식)가 낫다.

### 10.2 확장 내 declarativeNetRequest

A안을 쓴다면 `adDomains`를 DNR 규칙(`resourceTypes: ["main_frame","sub_frame"]`, `action: {type:"block"}`)으로 만들어 광고 페이지가 **로드되기 전에** 죽일 수 있다. 탭은 남지만 트래픽/CPU를 아낀다. CDP 방식에서는 `Fetch` 도메인이나 선차단 모드가 같은 역할을 한다.

### 10.3 Win32 폴백

확장·CDP가 못 잡는 경우(다른 프로필의 Chrome, 확장 비활성 등)를 위한 최후 수단이다.

```csharp
[DllImport("user32.dll")]
static extern IntPtr SetWinEventHook(uint eventMin, uint eventMax, IntPtr hmod,
    WinEventDelegate cb, uint idProcess, uint idThread, uint dwFlags);
const uint EVENT_OBJECT_SHOW = 0x8002;
const uint WINEVENT_OUTOFCONTEXT = 0x0000, WINEVENT_SKIPOWNPROCESS = 0x0002;

// 콜백에서:
//  1) GetClassName(hwnd) == "Chrome_WidgetWin_1" 인지
//  2) GetWindowThreadProcessId → 프로세스명이 chrome.exe 인지
//  3) 창 크기/스타일로 팝업 창인지
//  4) UI Automation(System.Windows.Automation)으로 주소창(ControlType.Edit)의
//     ValuePattern 에서 URL 취득
//  5) 블랙리스트 매칭 시 PostMessage(hwnd, WM_CLOSE, 0, 0)
```

UI Automation 조회는 수십~수백 ms가 걸리고 Chrome은 접근성 트리를 지연 생성한다(`--force-renderer-accessibility` 없이는 주소창 값이 비어 있을 수 있음). 오탐 시 정상 창을 닫아버릴 수 있으므로 **엄격한 도메인 매칭 시에만 닫거나 확인 알림 후 닫기**로 제한해야 한다.

---

## 11. 테스트 계획

### 11.1 자동 테스트

```powershell
dotnet run --project src -c Release -- --self-test   # 판정 점수 단위 검사 16개
node .\tests\browser-smoke.mjs                        # 실제 headless Chrome 스모크 테스트 5개 (Node.js 22 이상)
```

자체 테스트는 `Score`, `ScoreRedirectHijack`, 클릭 의도 대조 같은 순수 계산만 검사한다. 스모크 테스트는 로컬 HTTP 서버와 headless Chrome을 띄워 실제 CDP 이벤트 흐름으로 다음을 확인한다. 보이는 링크로 연 탭 유지, 링크 클릭과 함께 열린 부수 광고 탭 종료, 버튼으로 연 일반 창 유지, 빈 영역 클릭으로 열린 광고 탭 종료, 3초 안에 쏟아진 광고 탭 12개 모두 종료다.

### 11.2 검증 항목

| 케이스 | 기대 결과 |
|---|---|
| 보이는 링크를 클릭해 연 외부 탭 | 유지 |
| 링크 클릭과 함께 별도로 열린 외부 탭 | 닫힘, 로그 기록, opener 탭 포커스 복귀 |
| 빈 영역·투명 링크 클릭으로 열린 외부 탭 | 닫힘 |
| 버튼으로 연 일반 외부 창 | 유지 (clicked-control −40) |
| 버튼으로 열렸어도 등록된 광고 도메인 | 닫힘 |
| 사용자 동작 없이 열린 외부 팝업 | 닫힘 |
| 같은 사이트 새 탭 | 유지 (same-site −35) |
| 주소창에 직접 입력한 새 탭 | 유지 (`no-opener`) |
| 로그인·결제 팝업 | 유지 (`whitelist` 또는 `sensitive-flow`) |
| 페이지 스크립트가 현재 탭을 다른 사이트로 이동 | 원래 URL로 복귀 |
| 주소창·북마크·뒤로 가기로 현재 탭 이동 | 유지 |
| 같은 페이지가 반복해서 리다이렉트 | 두 번 되돌린 뒤 허용 |
| 짧은 시간에 광고 탭 12개 발생 | 모두 닫힘 |
| 선차단 모드에서 정상 탭 열기 | 1.6초 안에 반드시 재개 |
| TabBouncer 재시작 후 기존 탭 | 새 팝업으로 보지 않고 유지 |
| 전용 Chrome 창을 모두 닫음 | 다시 띄우지 않고 "Chrome 열기" 대기 |
| `config.json` 수정 | 즉시 반영 (hot-reload), 감시 켜짐·꺼짐은 유지 |
| 감시 꺼짐 | GUI 배너·창 제목·안내 페이지에 표시 |
| 창에 일반 탭이 하나뿐일 때 판정 히트 | 닫지 않음 + 경고 |

### 11.3 로그 분석

`events.jsonl`은 한 줄에 하나의 JSON이므로 PowerShell로 바로 집계할 수 있다.

```powershell
Get-Content "$env:LOCALAPPDATA\TabBouncer\events.jsonl" |
  ForEach-Object { ($_ | ConvertFrom-Json).data } |
  Group-Object reason | Sort-Object Count -Descending |
  Format-Table Count, Name
```

관측 모드로 지켜보는 동안 이 결과를 보고 `closeThreshold`와 목록을 조정한다. 사람이 읽는 활동 기록은 같은 폴더의 `tabbouncer.log`에 남는다.

### 11.4 네이티브 호스트 단독 디버깅 (A안)

```python
# tools/probe.py
import struct, subprocess, json
p = subprocess.Popen(["tabbouncer-host.exe"], stdin=subprocess.PIPE, stdout=subprocess.PIPE)
def send(o):
    b = json.dumps(o).encode()
    p.stdin.write(struct.pack("<I", len(b)) + b); p.stdin.flush()
def recv():
    n = struct.unpack("<I", p.stdout.read(4))[0]
    return json.loads(p.stdout.read(n))
send({"type":"hello","version":"0.1.0"}); print(recv())
send({"id":1,"type":"evaluate","ctx":{"openerUrl":"https://problem-site.com/x",
      "finalUrl":"https://popads.net/y","windowType":"popup","active":False,
      "initialUrl":"about:blank","redirects":2,"dtClick":300}})
print(recv())
```

---

## 12. 배포와 설치

**B안(채택)** — `dotnet publish` 결과인 `tabbouncer.exe`와 `config.json`을 같은 폴더에 두고 실행한다. 설정은 실행 파일 옆 `config.json`에 두고(없으면 기본값으로 만든다), 로그(`tabbouncer.log`, `events.jsonl`)·안내 페이지(`start.html`)·전용 프로필은 `%LOCALAPPDATA%\TabBouncer\`에 생성된다. `SelfContained=false`라 .NET 10 Desktop Runtime이 필요하다. Windows GUI(WinExe)라 콘솔 창은 뜨지 않는다. 자동시작이 필요하면 작업 스케줄러에 로그온 트리거로 등록하거나 시작 프로그램 폴더(`shell:startup`)에 바로가기를 넣는다. 다만 감시는 실행할 때마다 GUI에서 직접 시작해야 한다.

**A안** — `install.ps1`이 (1) `C:\Program Files\TabBouncer\`에 호스트와 매니페스트 복사, (2) 매니페스트 `path` 치환, (3) `HKCU\...\NativeMessagingHosts\com.tabbouncer.host` 등록, (4) 기본 `config.json` 생성, (5) 확장 폴더 배치 및 UI 로드 안내, (6) 확장 ID 확인 후 `allowed_origins` 갱신, (7) 트레이 자동시작 등록 순으로 수행한다. `uninstall.ps1`은 역순 + 레지스트리 키 삭제.

---

## 13. 트러블슈팅

| 증상 | 원인 | 해결 |
|---|---|---|
| 광고 탭이 전혀 안 닫히고 "최근 차단한 탭"이 비어 있음 | 평소 쓰는 Chrome에서 사이트를 보고 있음, 또는 감시가 꺼져 있음 | 전용 Chrome 창(안내 페이지가 뜬 창)에서 사이트를 연다. GUI 배너·안내 페이지로 감시 상태를 확인하고 "감시 시작"을 누른다 |
| 내용 없는 Chrome 창이 뜸 | TabBouncer 전용 Chrome | 정상이다. 이 창에서 브라우징한다 |
| 전용 창을 닫았더니 감시가 멈춤 | 전용 Chrome이 종료돼 감시 대상이 없음 | GUI의 "Chrome 열기" |
| `127.0.0.1:9222`에 연결 실패 | Chrome 136+에서 기본 프로필로 디버깅 포트를 열려 함 | `--user-data-dir`이 비표준 경로인지 확인. 이미 기본 프로필 Chrome이 떠 있으면 새 인스턴스가 안 뜨므로 전부 종료 후 재시도 |
| 새 탭이 잠깐 멈춘 것처럼 보임 | 선차단 모드의 정지 해제 지연 | 스위퍼가 1.6초에 강제 해제하지만, 거슬리면 `--no-preempt` |
| 정상 탭이 닫힘 | 룰 과적합 | 관측 모드로 전환 → `events.jsonl` 분석 → `watchedSites` 좁히기, GUI의 "정상 사이트로 등록" |
| 정상적인 페이지 이동이 원래 페이지로 되돌아감 | 클릭 없이 다른 사이트로 가는 정상 서비스(세션 만료 안내 등) | 목적지를 `allowedSites`에 등록하거나 `blockRedirectHijack: false` |
| 전용 창인데 아무것도 안 닫힘 | 관측 모드이거나 점수가 기준 미달 | 설정 확인. `events.jsonl`의 회색지대 점수를 보고 `closeThreshold`·`adDomains` 조정 |
| 탭은 닫히는데 광고 소리/영상이 잠깐 남 | 선차단 미적용 경로 | `preemptiveBlock: true` 확인, `adDomains` 보강 |
| 사이트가 "팝업 차단됨"이라며 콘텐츠를 안 보여줌 | 안티-애드블록 | 열리게 두고 즉시 닫는 현 설계가 유리. `debounceMs`를 300~800으로 낮춤 |
| 브라우저가 통째로 닫힘 | 마지막 탭을 닫음 | 최신 코드에는 `normalPages <= 1` 가드가 있음. 확인 |
| (A안) `native messaging host not found` | 레지스트리 키/매니페스트 경로 오류 | Process Monitor로 레지스트리 조회 추적 |
| (A안) `access to host is forbidden` | `allowed_origins` 확장 ID 불일치 | 매니페스트에 `key` 넣어 ID 고정 |
| (A안) 호스트가 즉시 죽음 | stdout에 로그 출력 | 모든 출력을 파일/stderr로 |
| (A안) 확장이 이벤트를 못 받음 | MV3 서비스 워커 유휴 종료 | 네이티브 포트 유지 + `chrome.alarms` 웨이크업, 상태는 `chrome.storage.session`에 |

---

## 14. 개발 일정

| Phase | 내용 | 예상 |
|---|---|---|
| 0 | 관측 전용 실행 + 로그 수집·분석 (Dry-run) | 0.5일 |
| 1 | §6 단일 파일 구현 및 빌드 | 1일 |
| 2 | 룰 튜닝, 목록 정비, 테스트 하네스 검증 | 1일 |
| 3 | 트레이화 / 자동시작 / 로그 뷰어 (선택) | 1일 |
| 4 | A안 전환 또는 Win32 폴백 추가 (선택) | 2일 |
| | **최소 동작까지** | **약 2~2.5일** |

---

## 15. 알려진 한계와 최종 권고

**전용 프로필**이 B안의 가장 큰 불편이다. 광고 차단은 TabBouncer가 연 전용 Chrome 창과 그 창에서 연 탭·창에서만 동작하고, **평소 쓰는 Chrome 창은 전혀 보호되지 않는다.** 평소 프로필의 확장·비밀번호도 따라오지 않으며, Chrome이 "자동화된 소프트웨어에 의해 제어되고 있습니다" 배너를 띄울 수 있다(기능 영향 없음). 평소 프로필을 그대로 감시해야 하면 §9의 A안으로 전환해야 한다.

**사후 처리 방식**이라는 한계도 있다. TabBouncer는 광고 스크립트가 실행된 뒤 생긴 탭을 닫거나 이동을 되돌린다. 스크립트 자체를 막는 광고 차단 확장보다 체감이 떨어질 수 있고, 되돌리기 전에 광고 페이지가 잠깐 보일 수 있다.

**버튼으로 위장한 광고**는 구분이 어렵다. 클릭 의도 추적(§5.4)으로 보이는 링크의 Ctrl·가운데 버튼 클릭까지 목적지를 대조해 보호하지만, 버튼이 연 창은 사람이 기대한 결과인지 코드만으로 알 수 없어 감점으로 보호한다. 그래서 가짜 버튼이 여는 광고는 `adDomains` 목록에 있어야 닫힌다.

**`Etld1()`은 Public Suffix List 전체가 아닌 간이 구현**이라 `github.io` 같은 특수 케이스에서 부정확하다. 정확도가 문제가 되면 PSL 파일을 내려받아 파싱하는 로직으로 교체하면 되고, 그만큼 데이터 파일이 하나 늘어난다.

**보안 주의.** 디버깅 포트가 열린 Chrome은 로컬 프로세스라면 누구나 제어할 수 있다. `--remote-debugging-address=127.0.0.1` 바인딩을 반드시 유지하고, 중요한 계정으로 로그인하는 프로필과는 분리해서 쓰기를 권한다. Chrome이 136부터 이 제약을 만든 이유가 바로 이것이다.

마지막으로 실용적인 권고 하나. 이 프로그램을 돌리기 전에 전용 프로필에 uBlock Origin Lite를 먼저 설치해 보시길 권한다. 대부분의 pop-under는 이미 필터 목록에 잡혀 있어 그것만으로 끝나는 경우가 많고, 그러면 TabBouncer는 잔여 팝업만 처리하는 가벼운 보조 장치가 되어 룰셋도 단순해지고 오탐 걱정도 거의 사라진다.

그리고 이 도구는 **본인 PC의 본인 브라우저**를 대상으로만 사용하시기 바란다. 타인·공용 PC에 브라우저를 조작하는 상주 프로그램을 심는 것은 별개의 문제이며, 네이티브 메시징 호스트와 디버깅 포트는 잘못 쓰면 그 자체가 보안 취약점이 된다.